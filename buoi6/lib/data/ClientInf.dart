class ClientInf{
  String name;
  int type;
  String phoneNumber;
  String city,district,address;
  String email;
  String taxCode;
  String idNumber;
  String description;
  ClientInf(this.name, this.type, this.phoneNumber, this.city, this.district,
      this.address, this.email, this.taxCode, this.idNumber, this.description);

  // ClientInf({required this.name, required this.type, required this.phoneNumber, required this.city, required this.district,
  //     required this.address, required this.email, required this.taxCode, required this.idNumber, required this.description});
}
